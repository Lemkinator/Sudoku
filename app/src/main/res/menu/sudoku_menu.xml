<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <group
        android:id="@+id/sudoku_menu_group_pause_play"
        android:visible="false">
        <item
            android:id="@+id/menu_pause_play"
            android:icon="@drawable/ic_oui_control_pause"
            android:title="@string/pause"
            app:iconTint="?actionMenuTextColor"
            app:showAsAction="always"
            tools:ignore="AppCompatResource" />
    </group>
    <group
        android:id="@+id/sudoku_menu_group_reset"
        android:visible="false">
        <item
            android:id="@+id/menu_reset"
            android:icon="@drawable/ic_oui_refresh"
            android:title="@string/restart"
            app:showAsAction="ifRoom"
            tools:ignore="AppCompatResource" />
    </group>
    <group
        android:id="@+id/sudoku_menu_group_share"
        android:visible="true">
        <item
            android:id="@+id/menu_share"
            android:icon="@drawable/ic_oui_share_outline"
            android:title="@string/share"
            app:showAsAction="ifRoom"
            tools:ignore="AppCompatResource" />
    </group>
</menu>