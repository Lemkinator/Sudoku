<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" /> <!-- Daily Sudoku notification. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!--
    TODO / Ideas

        max hints -> difficulty? / 5?
        setting: directly show errors
        autonote: toggle
        show numbers left on button

        play integration (achievements, leaderboards, etc.)
        multiplayer

        landscape mode?
        (splash icon on non sammy devices squarish?) check splash icon on non sammy

        (new Android 12 Splashscreen)
    -->

    <application
        android:name=".App"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:enableOnBackInvokedCallback="false"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:localeConfig="@xml/locale_config"
        android:resizeableActivity="true"
        android:supportsRtl="true"
        android:theme="@style/Theme.Sudoku"
        tools:targetApi="33">

        <!-- SamsungBasicInteraction -->
        <meta-data
            android:name="SamsungBasicInteraction"
            android:value="SEP10" />
        <!-- Samsung Icon flags -->
        <meta-data
            android:name="com.samsung.android.icon_container.has_icon_container"
            android:value="true" />
        <meta-data
            android:name="com.samsung.android.icon_container.feature_appicon"
            android:value="ADAPTIVEICON_SHADOW" />

        <meta-data
            android:name="theming-meta"
            android:value="meta_998_sesl_app" />

        <meta-data
            android:name="theming-meta-xml"
            android:value="@xml/meta_998_sesl_app" />

        <activity
            android:name=".ui.SettingsActivity"
            android:parentActivityName=".ui.MainActivity" />
        <activity android:name=".ui.AboutActivity" />
        <activity android:name=".ui.AboutMeActivity" />
        <activity
            android:name=".ui.DailySudokuActivity"
            android:parentActivityName=".ui.MainActivity" />
        <activity
            android:name=".ui.SudokuLevelActivity"
            android:parentActivityName=".ui.MainActivity" />
        <activity
            android:name=".ui.SudokuActivity"
            android:screenOrientation="portrait"
            tools:ignore="LockedOrientationActivity" />
        <activity
            android:name=".ui.MainActivity"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:exported="true" />
        <activity
            android:name=".ui.OOBEActivity"
            android:screenOrientation="portrait"
            tools:ignore="LockedOrientationActivity" />
        <activity
            android:name=".ui.SplashActivity"
            android:exported="true"
            android:noHistory="true">
            <!--Launcher-->
            <meta-data
                android:name="com.sec.android.app.launcher.icon_theme"
                android:value="themeColor" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".ui.utils.AlarmReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>